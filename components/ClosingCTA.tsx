export function ClosingCTA() {
  return (
    <section className="w-full border-t border-white/10 bg-[#050818] px-6 py-24 text-center">
      <p className="text-xs uppercase tracking-[0.3em] text-cyan-300/70">
        This was the concept, not the pitch
      </p>
      <h2 className="mx-auto mt-4 max-w-2xl text-3xl font-semibold text-white sm:text-4xl">
        Everything above is real, working code — swap in your photos,
        characters, and crew, and this is the site.
      </h2>
      <p className="mx-auto mt-4 max-w-xl text-sm text-white/60">
        Let&rsquo;s talk through what it takes to get the rest of the
        building built out.
      </p>
      <a
        href="mailto:hello@example.com"
        className="mt-8 inline-block rounded-full bg-cyan-400 px-8 py-3 text-sm font-semibold text-[#050818] transition hover:bg-cyan-300"
      >
        Let&rsquo;s build this
      </a>
    </section>
  );
}
